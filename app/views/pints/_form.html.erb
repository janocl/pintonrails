<%= form_for(@pint) do |f| %>
  <% if @pint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pint.errors.count, "error") %> prohibited this pint from being saved:</h2>

      <ul>
      <% @pint.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, "Titulo" %><br>
    <%= f.text_field :title, :placeholder => "Nombre..." %>
  </div>
  <div class="field">
    <%= f.label :description, "Descripción" %><br>
    <%= f.text_field :description, :placeholder => "Descripción..." %>
  </div>
  <div class="field">
    <%= f.label :image, "Imagen" %><br>
    <%= f.text_field :image, :placeholder => "Link Imagen..." %>
  </div>
  <div class="field">
    <%= f.label :biography, "Biografía" %><br>
    <%= f.text_area :biography %>
  </div>
  <div class="actions">
    <%= f.submit @pint.new_record? ? "Crear Pint!" : "Actualizar" %>
  </div>
<% end %>
